version: '3'
services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - '8080:8080'
    volumes:
      - 'jenkins_home:/var/jenkins_home'
    restart: always

  # ToDo add nginx with ssl

volumes:
  jenkins_home:
